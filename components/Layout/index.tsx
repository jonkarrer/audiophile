import Header from "./Header";
import Footer from "./Footer";
import { Children } from "../../utils/interfaces";

const Layout = ({ children }: Children) => <div>{children}</div>;

export default Layout;
